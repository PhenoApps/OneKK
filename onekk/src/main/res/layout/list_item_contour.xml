<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="android.view.View"/>

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener"/>
        <variable
            name="model"
            type="org.wheatgenetics.onekk.database.models.ContourEntity"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{clickListener}">

        <CheckBox
            android:id="@+id/checkbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="@={model.selected}"
            android:clickable="false"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <TextView style="@style/Text.ExperimentTitle"
            android:id="@+id/areaTextView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:text="@{String.valueOf(model.contour.area) + @string/contour_list_area_suffix}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/minAxisTextView"
            app:layout_constraintStart_toEndOf="@id/checkbox"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView style="@style/Text.ExperimentTitle"
            android:id="@+id/minAxisTextView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:text='@{!model.contour.isCluster() ? String.valueOf(model.contour.minAxis) + "x" + String.valueOf(model.contour.maxAxis) : String.valueOf(model.contour.count)}'
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/areaTextView"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:background="#FF000000"
            android:layout_height="2dp"
            android:layout_width="match_parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/checkbox"/>

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>